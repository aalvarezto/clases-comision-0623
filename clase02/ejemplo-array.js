"use strict"

const frutas = ["Manzana", "Naranja", "Banana", "Ciruela", "Frutilla"];

let indice = 0

while (indice < frutas.length) {
	const fruta = frutas[indice];
	
	console.log(fruta);
	indice++
}


for (let indice = 0; indice < frutas.length; indice++) {
	const fruta = frutas[indice];
	
	console.log(fruta);
}
